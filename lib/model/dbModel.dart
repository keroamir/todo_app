class DBModel{
  var description;
  var title;
  var date;
  var id;
  var done;

  DBModel({required this.description,required this.title,required this.date, required this.id, required this.done});
}